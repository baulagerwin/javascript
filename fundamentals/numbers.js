// Numbers

let firstNumber = 100;
let secondNumber = 200;

result = firstNumber + secondNumber;
result = firstNumber - secondNumber;
result = firstNumber - 0.1;
result = firstNumber * 5;
result = firstNumber / 5;

result = result.toFixed(2);

// Math Object

result = Math.PI;
result = Math.E;
result = Math.round(5.4);
result = Math.ceil(5.2);
result = Math.floor(5.2);

result = Math.sqrt(36);
result = Math.pow(6, 2);
result = Math.abs(-5);

result = Math.min(2, 3, 5, 6, -1);
result = Math.max(2, 3, 5, 6, -1);
result = Math.random();

result = Math.round(Math.random() * 10);

console.log(result);
